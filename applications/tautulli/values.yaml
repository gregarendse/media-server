image:
    repository: linuxserver/tautulli
    # Overrides the image tag whose default is the chart appVersion.
    tag: "latest"
    pullPolicy: Always

volumes:
    config:
        source: "/home/docker/tautulli/config"
        dest: "/config"

environment:
    - name: PUID
      value: "113"
    - name: GUID
      value: "117"
    - name: TZ
      value: Africa/Johannaesburg

ports:
    http:
        target: 8181
        protocol: TCP

ingress:
  annotations:
#     kubernetes.io/ingress.class: public
#     kubernetes.io/tls-acme: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
  hosts:
    - host: tautulli.lan
      path: /
      service:
        name: http
    - host: tautulli.arendse.nom.za
      tls: true
      path: /
      service:
        name: http
    - host: 6f3806410f7b.sn.mynetname.net
      tls: true
      path: /tautulli
      service:
        name: http
